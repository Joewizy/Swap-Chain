import { Placeholder } from "@/components/Placeholder";

/** Order history — reads existing endpoints in Phase 1. */
export function HistoryScreen() {
  return (
    <Placeholder
      title="History"
      subtitle="Past orders and their status timelines. Reads existing backend endpoints in Phase 1."
    />
  );
}
