import { Placeholder } from "@/components/Placeholder";

/** Saved recipients — banks, mobile-money providers, wallet addresses. */
export function RecipientsScreen() {
  return (
    <Placeholder
      title="Recipients"
      subtitle="Saved people and payout destinations (Opay, M-Pesa, GTBank, wallets). Phase 1."
    />
  );
}
