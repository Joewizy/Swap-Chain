import { Placeholder } from "@/components/Placeholder";

/** Home — natural-language intent chat. Wired to /api/chat in Phase 1. */
export function IntentScreen() {
  return (
    <Placeholder
      title="Ask Railglide"
      subtitle="Plain-English intent chat lands here in Phase 1 — routes to /api/chat, then hands off to a guided flow."
    />
  );
}
