# ---------------------------------------------------------------------------
# Network universe — flips testnet ⇄ mainnet across every rail.
# See ARCHITECTURE.md §4.
# ---------------------------------------------------------------------------
NEXT_PUBLIC_NETWORK=testnet


# ---------------------------------------------------------------------------
# Wallet (RainbowKit / WalletConnect)
# ---------------------------------------------------------------------------
NEXT_PUBLIC_WALLET_CONNECT_ID=your_wallet_connect_project_id_here


# ---------------------------------------------------------------------------
# AI / intent parser
# ---------------------------------------------------------------------------
# Defaults to GitHub Models inference if OPENAI_BASE_URL is unset:
#   https://models.github.ai/inference
# Override with https://api.openai.com/v1 to use OpenAI directly.
OPENAI_API_KEY=
# OPENAI_BASE_URL=
# OPENAI_MODEL=openai/gpt-4o-mini


# ---------------------------------------------------------------------------
# Phase 1 placeholders — uncomment as the rails come online.
# ---------------------------------------------------------------------------

# Chainrails (inbound + fiat on-ramp)
# CHAINRAILS_API_KEY=
# CHAINRAILS_BASE_URL=https://api.chainrails.io

# Paycrest (fiat off-ramp to bank / mobile money)
# Mainnet only — no sandbox. Get a key from https://app.paycrest.io
# PAYCREST_API_KEY=
# Secret used to verify the X-Paycrest-Signature on inbound webhooks (HMAC-SHA256
# over the raw body). This is the Paycrest *API Secret / Client Secret* from the
# Sender dashboard — NOT the same value as PAYCREST_API_KEY. Register the webhook
# URL (https://<host>/api/paycrest/webhook) in the Sender dashboard.
# PAYCREST_WEBHOOK_SECRET=
# Shared secret for the reconcile cron (GET /api/paycrest/reconcile). Vercel Cron
# sends it as `Authorization: Bearer <CRON_SECRET>`; the route refuses to run
# without it. Generate any long random string. NOTE: sub-daily Vercel crons need
# a Pro plan — on Hobby, drive the same endpoint from an external scheduler
# (e.g. a GitHub Actions cron) with this header instead.
# CRON_SECRET=

# Circle CCTP (USDC ↔ USDC outbound)
# CIRCLE_API_KEY=

# Relay attribution (optional — lets Relay credit volume to our app)
# NEXT_PUBLIC_RELAY_APP_ID=


# ---------------------------------------------------------------------------
# Optional private RPCs
# ---------------------------------------------------------------------------
# NEXT_PUBLIC_BASE_RPC_URL=
# NEXT_PUBLIC_STARKNET_RPC_URL=
# Solana RPC for the Relay SVM wallet (Phantom etc). Powers Solana balance
# reads in the swap widget as well as transaction sending. Falls back to the
# public cluster endpoint, which is rate-limited — set a dedicated node (Helius/
# QuickNode/Alchemy) for reliable SPL-token balance lookups in prod.
# NEXT_PUBLIC_SOLANA_RPC=

# ARGENT_PRIVATE_KEY is intentionally absent — server-side custody was
# removed in Phase 0a. Starknet swaps will be re-added in Phase 2 with
# client-side signing via Argent / Braavos.
